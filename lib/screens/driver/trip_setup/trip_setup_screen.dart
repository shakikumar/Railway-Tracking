import 'package:flutter/material.dart';

class TripSetupScreen extends StatelessWidget {
  const TripSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Trip Setup'),
      ),
    );
  }
}
