import 'package:flutter/material.dart';

class StationMasterDashboardScreen extends StatelessWidget {
  const StationMasterDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Station Master Dashboard'),
      ),
    );
  }
}
