import 'package:flutter/material.dart';

class BleConfigScreen extends StatelessWidget {
  const BleConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BLE Station Config'),
      ),
    );
  }
}
